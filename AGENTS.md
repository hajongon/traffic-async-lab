# AGENTS.md

이 저장소에서 작업하는 에이전트는 아래 운영 규칙을 따른다.

## 작업 기록

요청을 처리할 때마다 작업 종료 전에 work-log를 남긴다.

- 저장 경로: `C:\Users\jongseung\obsidian-sync\01_Projects\202605090_traffic-async-lab\work-log`
- 파일에는 다음 항목을 포함한다.
  - 사용자가 지시한 사항
  - 수행한 내용
  - 산출물
  - 우려되는 사항
  - 후속 작업 계획
- 파일명은 작업 내용을 식별할 수 있도록 시간과 간단한 주제를 포함한다.
- 파일명 시간 형식은 `YY-MM-DD HH-MM-SS`를 사용한다.

## 지식 기록

요청마다 해당 작업과 관련하여 백엔드 개발자로서 알아두면 좋은 지식을 별도 파일로 작성한다.

- 저장 경로: `C:\Users\jongseung\obsidian-sync\01_Projects\202605090_traffic-async-lab\지식`
- 내용에는 가능한 범위에서 다음 관점을 포함한다.
  - 왜 해당 기술이나 접근을 선택했는지
  - 고려할 수 있는 다른 후보는 무엇이었는지
  - 해당 기술의 핵심 컨셉은 무엇인지
  - 실무에서 주의할 점은 무엇인지
- 파일명은 work-log와 연결해 추적할 수 있도록 시간과 주제를 포함한다.
- 파일명 시간 형식은 `YY-MM-DD HH-MM-SS`를 사용한다.

## 적용 원칙

- 기록은 간결하되 나중에 작업 맥락을 복원할 수 있을 정도로 구체적으로 작성한다.
- 코드 변경, 테스트 실행, 설정 변경, 문서 작성 등 의미 있는 작업 단위마다 기록을 남긴다.
- 우려되는 사항이 없더라도 "없음" 또는 "현재 확인된 사항 없음"으로 명시한다.

## 커밋 규칙

작업 하나를 수행할 때마다 의미 있는 단위로 commit을 남긴다.

- 커밋 전에는 변경 범위를 확인하고, 작업과 무관한 로컬 산출물은 포함하지 않는다.
- 커밋 메시지는 작업 내용을 짧고 구체적으로 설명한다.
- work-log와 지식 기록은 요청한 외부 Obsidian 경로에 남기며, 저장소 커밋에는 포함하지 않는다.
- 사용자가 push를 요청한 경우 원격 저장소 상태를 확인한 뒤 현재 브랜치를 push한다.
